---
import '../styles/global.css';
import SEO from '../components/SEO.astro';
import ThemeToggle from '../components/ThemeToggle.astro';

interface Props {
  title: string;
  description?: string;
  coverImage?: string;
  date?: string | Date;
}

const {
  title,
  description = "The Performance Book - Squeezing every drop of efficiency out of software.",
  coverImage,
  date,
} = Astro.props;

// Ensure base URL pathing is correct
const baseUrl = import.meta.env.BASE_URL.replace(/\/$/, '');
---

<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href={`${baseUrl}/favicon.svg`} />
    
    <SEO title={title} description={description} coverImage={coverImage} date={date} />
    
    <!-- GoatCounter Analytics (Production Only) -->
    {import.meta.env.PROD && (
      <script is:inline data-goatcounter="https://theperformancebook.goatcounter.com/count" async src="//gc.zgo.at/count.js"></script>
    )}
    
    <!-- FOUC-Resistant Theme Script -->
    <script is:inline>
      const theme = (() => {
        if (typeof localStorage !== 'undefined' && localStorage.getItem('theme')) {
          return localStorage.getItem('theme');
        }
        if (window.matchMedia('(prefers-color-scheme: dark)').matches) {
          return 'dark';
        }
        return 'light';
      })();
      
      if (theme === 'light') {
        document.documentElement.classList.remove('dark');
      } else {
        document.documentElement.classList.add('dark');
      }
    </script>
    
    <!-- Google Fonts: Outfit (Headings) and Inter (Body) -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Outfit:wght@400;500;600;700;800&family=Fira+Code:wght@400;500&display=swap" rel="stylesheet" />

    <!-- KaTeX CSS for Math Rendering -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/katex@0.17.0/dist/katex.min.css" integrity="sha384-vlBdW0r3AcZO/HboRPznQNowvexd3fY8qHOWkBi5q7KGgqJ+F48+DceybYmrVbmB" crossorigin="anonymous" />
  </head>
  <body class="bg-bg-base text-text-base font-sans antialiased selection:bg-bg-surface/30 selection:text-text-base min-h-screen flex flex-col transition-colors duration-200">
    <!-- Premium Header -->
    <header class="border-b border-border-muted bg-bg-base/80 backdrop-blur-md sticky top-0 z-40 transition-colors duration-200">
      <div class="max-w-4xl mx-auto px-6 h-16 flex items-center justify-between">
        <a href={`${baseUrl}/`} class="group flex items-center gap-2.5 font-display font-bold text-xl tracking-tight text-text-base">
          <div class="w-8 h-8 rounded-lg bg-bg-surface flex items-center justify-center shadow-lg shadow-bg-surface/20 group-hover:scale-105 transition-transform duration-200">
            <span class="text-bg-base text-base font-extrabold font-display">P</span>
          </div>
          <span class="text-text-base group-hover:text-text-muted transition-colors duration-200">
            The Performance Book
          </span>
        </a>
        
        <nav class="flex items-center gap-6">
          <a href={`${baseUrl}/`} class="text-sm font-medium text-text-muted hover:text-text-base transition-colors duration-200">
            Articles
          </a>
          <a href={`${baseUrl}/favorites/`} class="text-sm font-medium text-text-muted hover:text-text-base transition-colors duration-200">
            Bookmarks
          </a>
          <a href="https://github.com" target="_blank" rel="noopener noreferrer" class="text-sm font-medium text-text-muted hover:text-text-base transition-colors duration-200 flex items-center gap-1">
            GitHub
          </a>
          <ThemeToggle />
        </nav>
      </div>
    </header>

    <!-- Main Content Container with max reading length limit -->
    <main class="flex-grow max-w-[65ch] w-full mx-auto px-6 py-12">
      <slot />
    </main>

    <!-- Footer -->
    <footer class="border-t border-border-muted bg-bg-base/50 py-8 text-center text-text-muted text-xs transition-colors duration-200">
      <div class="max-w-4xl mx-auto px-6 flex flex-col md:flex-row items-center justify-between gap-4">
        <p>© {new Date().getFullYear()} The Performance Book. All rights reserved.</p>
        <p class="flex gap-4">
          <a href={`${baseUrl}/`} class="hover:text-text-base transition-colors">Home</a>
          <span class="text-border-muted">•</span>
          <a href="https://github.com" class="hover:text-text-base transition-colors">GitHub</a>
        </p>
      </div>
    </footer>
  </body>
</html>

<style is:global>
  /* Base Typography Overrides for Premium Aesthetic */
  h1, h2, h3, h4, h5, h6 {
    font-family: 'Outfit', sans-serif;
    color: var(--color-text-base);
    font-weight: 700;
    letter-spacing: -0.025em;
  }
  
  body {
    font-family: 'Inter', sans-serif;
  }

  code, pre {
    font-family: 'Fira Code', monospace;
  }

  /* Styling KaTeX formula blocks for clean presentation */
  .katex-display {
    padding: 1.25rem;
    background-color: color-mix(in srgb, var(--color-bg-surface) 10%, transparent);
    border: 1px solid var(--color-border-muted);
    border-radius: 0.75rem;
    overflow-x: auto;
    margin: 1.5rem 0;
    box-shadow: inset 0 2px 4px 0 rgba(0, 0, 0, 0.02);
  }

  .katex {
    font-size: 1.05em;
    color: var(--color-text-base);
  }

  /* Shiki Code Block Styles */
  pre {
    padding: 1.25rem;
    border-radius: 0.75rem;
    border: 1px solid var(--color-border-muted);
    margin: 1.5rem 0;
    font-size: 0.9em;
    line-height: 1.6;
    overflow-x: auto;
    background-color: #0d1117 !important;
  }

  /* Article prose styles to simulate tailwind typography */
  .prose p {
    margin-bottom: 1.25rem;
    line-height: 1.75;
    color: var(--color-text-base);
  }

  .prose h2 {
    font-size: 1.65rem;
    margin-top: 2rem;
    margin-bottom: 1rem;
    border-bottom: 1px solid var(--color-border-muted);
    padding-bottom: 0.4rem;
  }

  .prose h3 {
    font-size: 1.35rem;
    margin-top: 1.75rem;
    margin-bottom: 0.75rem;
  }

  .prose ul {
    list-style-type: disc;
    padding-left: 1.5rem;
    margin-bottom: 1.25rem;
    color: var(--color-text-base);
  }

  .prose ol {
    list-style-type: decimal;
    padding-left: 1.5rem;
    margin-bottom: 1.25rem;
    color: var(--color-text-base);
  }

  .prose li {
    margin-bottom: 0.5rem;
    line-height: 1.625;
  }

  .prose a {
    color: var(--color-text-muted);
    text-decoration: underline;
    text-underline-offset: 4px;
    font-weight: 500;
    transition: color 0.15s ease;
  }

  .prose a:hover {
    color: var(--color-text-base);
  }

  .prose blockquote {
    border-left: 4px solid var(--color-border-muted);
    padding-left: 1.25rem;
    font-style: italic;
    color: var(--color-text-muted);
    margin: 1.5rem 0;
  }
</style>
